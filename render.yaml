services:
  - type: web
    name: newsapp
    env: node
    plan: free
    buildCommand: "npm run render-build"
    startCommand: "npm run server"
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: NEWS_API_KEY
        sync: false
